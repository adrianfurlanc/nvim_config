" vim-session (not installed yet — see the plugin list in autoload/pack.vim)
let g:session_autosave='no'
let g:session_autoload='no'
let g:session_command_aliases = 1
let g:session_directory='~/.config/nvim/sessions'
