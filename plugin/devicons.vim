" Tells the vim-devicons plugin to add Unicode folder icons (📁-style glyphs
" from a Nerd Font) to directory nodes in NERDTree, so folders are visually
" distinguishable from files.
let g:WebDevIconsUnicodeDecorateFolderNodes = 1
