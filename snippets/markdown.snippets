# Custom Markdown snippets (snipmate format, read by coc-snippets).
# Body lines must be tab-indented. $N = tab stop, $0 = final cursor stop.

snippet post "Blog post front matter (adrianfurlan.com)"
	---
	title: "$1"
	date: "$2"
	image: $3
	excerpt: "$4"
	caption: "$5"
	---

	$0
